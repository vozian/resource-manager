# Mock Data Instructions

I need the following parameter types:

- category: string // this could be "material", "machine"
- materialType: string // this could be "DNA", "RNA", "protein"
- machineType: string // this could be "centrifuge", "liquid handler"
- dnaCode: string // this is a string of letters representing the DNA code, e.g. "ATCG"
- dnaConcentration: number // this is a number representing the concentration of the DNA, e.g. 0.5
-
