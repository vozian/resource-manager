# Mock Data Instructions

I need the following parameter types:

- category: string // this could be "material", "machine"
- materialType: string // this could be "Twist DNA", "Diluted DNA", "Protein"
- machineType: string // this could be "expression machine", "liquid handler", "BLI machine"
- temperature: number // this is a number representing the temperature in degrees Celsius, e.g. 37
- duration: number // this is a number representing the duration in seconds, e.g. 3600
- dnaCode: string // this is a string of letters representing the DNA code, e.g. "ATCG"
- wellCount: number
- concentration: number

Figure out some resources and jobs based on the above.
